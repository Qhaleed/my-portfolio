export default function () {
  return (
    <nav className="mt-3 sm:mt-5 w-full flex justify-center">
      <button className="rounded-full bg-gray-200 w-10 h-10 sm:w-12 sm:h-12 border border-gray-300 hover:bg-gray-300 transition-colors duration-200 flex items-center justify-center">
        <i className="fa-solid fa-house text-black text-sm sm:text-base"></i>
      </button>
    </nav>
  );
}
